<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:gravity="center"
    xmlns:fab="http://schemas.android.com/tools"
    android:orientation="vertical">

    <TextView
        android:id="@+id/expense_attandance"
        android:layout_width="match_parent"
        android:text="@string/expense_report"
        android:textStyle="bold"
        android:visibility="gone"
        android:textColor="@color/colorPrimary"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_height="wrap_content" />

    <ProgressBar
        android:layout_width="match_parent"
        android:id="@+id/expense_at_list"
        android:layout_below="@id/expense_attandance"
        android:layout_height="30dp" />

    <ListView
        android:id="@+id/expense__list"
        android:layout_width="match_parent"
        android:layout_marginTop="20dp"
        android:scrollbars="none"
        android:layout_below="@id/expense_at_list"
        android:layout_height="wrap_content" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/expense_staff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:clickable="true"
        fab:fab_colorNormal="@color/colorPrimaryDark"
        fab:fab_colorPressed="@color/ColorPrimaryDark"
        fab:fab_colorRipple="@color/ColorPrimary"
        android:src="@drawable/ic_refresh"
        app:elevation="4dp"/>

</RelativeLayout>